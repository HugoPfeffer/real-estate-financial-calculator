import { Select as SelectPrimitive } from 'bits-ui';

const Root = SelectPrimitive.Root;
const Trigger = SelectPrimitive.Trigger;
const Content = SelectPrimitive.Content;
const Item = SelectPrimitive.Item;
const Group = SelectPrimitive.Group;
const GroupHeading = SelectPrimitive.GroupHeading;

export { Root, Trigger, Content, Item, Group, GroupHeading, Root as Select };
